<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapReply">
	<select id="replyList" parameterType="Integer" resultType="ALReply">
		select * from reply where bo_id = #{bo_id}
	</select>
	
	<insert id="insertBoard" parameterType="ALReply">
		여기수정boards_SEQ.nextval, #{mem_id}, #{title}, #{context}, #{be_image}, #{af_image}, sysdate)
	</insert>

</mapper>